apiVersion: skupper.io/v2alpha1  
kind: MultiKeyListener  
metadata:  
  name: mkl-backend  
spec:  
  host: 0.0.0.0  
  port: 19092  
  strategy:  
    weighted:  
      routingKeys:  
        podman-backend: 1  
---  
apiVersion: skupper.io/v2alpha1  
kind: MultiKeyListener  
metadata:  
  name: mkl-backend-http  
spec:  
  host: 0.0.0.0  
  port: 19091  
  strategy:  
    weighted:  
      routingKeys:  
        podman-backend-http: 1