# Get-Back runtime dependencies
# Python 3.11+ standard library provides most functionality:
# - asyncio (concurrent servers)
# - logging (observability)
# - argparse (CLI)
# - socket (networking)

# Required for OpenAPI spec loading
PyYAML>=6.0
